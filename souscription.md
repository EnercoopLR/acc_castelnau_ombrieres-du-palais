---
title: "Souscription"
order: 6
in_menu: true
---
# JE SOUSCRIS À UNE BOUCLE D'AUTOCONSOMMATION COLLECTIVE DU TERRITOIRE 

***

## À Castelnau-Le-Lez - Les ombrières du Palais
Je me renseigne pour savoir si je suis éligible géographiquement :

<link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css"> <style> .acc-map-container { width: 100%; max-width: 900px; margin: 30px auto; background: #f3f5f7; border-radius: 16px; box-shadow: 0 10px 30px rgba(0, 0, 0, .15); overflow: hidden; } .acc-map-header { padding: 20px; background: white; border-bottom: 1px solid #ddd; } .acc-map-container input { width: 100%; padding: 14px; font-size: 16px; border-radius: 8px; border: 1px solid #bbb; box-sizing: border-box; } .acc-map-suggestions { position: relative; } .acc-map-list { position: absolute; left: 0; right: 0; background: white; border-radius: 8px; box-shadow: 0 5px 15px rgba(0, 0, 0, .2); z-index: 1000; overflow: hidden; } .acc-map-item { padding: 10px; cursor: pointer; } .acc-map-item:hover { background: #efefef; } #map { height: 500px; width: 100%; } .acc-map-result { padding: 20px; background: white; font-size: 18px; font-weight: bold; } .acc-map-result .ok { color: #0b8d3f; } .acc-map-result .ko { color: #d62828; } .acc-map-result .distance { margin-top: 10px; font-size: 15px; font-weight: normal; } .acc-map-container .green-marker { filter: hue-rotate(-70deg) saturate(1.2); } /* Adaptation mobile */ @media (max-width: 700px) { .acc-map-container { width: 100%; margin: 20px 0; border-radius: 10px; } #map { height: 400px; } } </style> <div class="acc-map-container">
<div class="acc-map-header">
    <input
        id="address"
        placeholder="Saisir une adresse..."
        autocomplete="off"
    >
    <div id="suggestions" class="acc-map-suggestions"></div>
</div>

<div id="map"></div>

<div class="acc-map-result" id="result">
    Entrez une adresse.
</div>

</div> <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script> <script> (function () { // ==================================================== // PARAMÈTRES // ==================================================== const CENTER = { lat: 43.63934, lon: 3.90812 }; const RAYON = 1000; // mètres const Coord_Ombrieres = { lat_omb: 43.645228, long_omb: 3.917201 }; // ==================================================== // INITIALISATION DE LA CARTE // ==================================================== const map = L.map('map').setView( [CENTER.lat, CENTER.lon], 14 ); L.tileLayer( "https://data.geopf.fr/wmts?" + "SERVICE=WMTS&REQUEST=GetTile&VERSION=1.0.0" + "&LAYER=GEOGRAPHICALGRIDSYSTEMS.PLANIGNV2" + "&STYLE=normal" + "&TILEMATRIXSET=PM" + "&FORMAT=image/png" + "&TILEMATRIX={z}" + "&TILEROW={y}" + "&TILECOL={x}" ).addTo(map); // ==================================================== // CERCLE D'ÉLIGIBILITÉ // ==================================================== L.circle( [CENTER.lat, CENTER.lon], { radius: RAYON, color: "#0077ff", fillColor: "#4ea3ff", fillOpacity: 0.15 } ).addTo(map); // ==================================================== // MARQUEUR DES OMBRIÈRES // ==================================================== const greenIcon = L.icon({ iconUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png', iconRetinaUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png', iconSize: [25, 41], iconAnchor: [12, 41], popupAnchor: [1, -34], className: 'green-marker' }); const PDL_ombrieres = L.marker( [ Coord_Ombrieres.lat_omb, Coord_Ombrieres.long_omb ], { title: "Poste transfo Ombrières du Palais", icon: greenIcon } ).addTo(map); PDL_ombrieres.bindPopup(` <div class="my-popup"> <strong> Poste de transformation des Ombrières du Palais </strong> <br> 500 kVA </div> `); // ==================================================== // MARQUEUR DE L'ADRESSE // ==================================================== let addressMarker = null; // ==================================================== // CALCUL DE DISTANCE — HAVERSINE // ==================================================== function distance(lat1, lon1, lat2, lon2) { const R = 6371000; const toRad = d => d * Math.PI / 180; const dLat = toRad(lat2 - lat1); const dLon = toRad(lon2 - lon1); const a = Math.sin(dLat / 2) ** 2 + Math.cos(toRad(lat1)) * Math.cos(toRad(lat2)) * Math.sin(dLon / 2) ** 2; return R * 2 * Math.atan2( Math.sqrt(a), Math.sqrt(1 - a) ); } // ==================================================== // AUTOCOMPLÉTION // ==================================================== const input = document.getElementById("address"); const suggestions = document.getElementById("suggestions"); let timeout = null; input.addEventListener("input", () => { clearTimeout(timeout); const txt = input.value.trim(); if (txt.length < 3) { suggestions.innerHTML = ""; return; } timeout = setTimeout( loadSuggestions, 250 ); }); async function loadSuggestions() { const txt = input.value.trim(); if (txt.length < 3) { suggestions.innerHTML = ""; return; } try { const params = new URLSearchParams({ text: txt, lonlat: `${CENTER.lon},${CENTER.lat}`, maximumResponses: 5 }); const url = `https://data.geopf.fr/geocodage/completion/?${params}`; const response = await fetch(url); if (!response.ok) { throw new Error( "Erreur lors de la recherche d'adresse" ); } const data = await response.json(); let html = "<div class='acc-map-list'>"; (data.results || []).forEach(item => { const address = item.fulltext || ""; const escapedAddress = address .replace(/\\/g, "\\\\") .replace(/'/g, "\\'"); html += ` <div class="acc-map-item" onclick="selectAccAddress('${escapedAddress}')" > ${address} </div> `; }); html += "</div>"; suggestions.innerHTML = html; } catch (error) { console.error( "Erreur autocomplétion :", error ); suggestions.innerHTML = ""; } } // ==================================================== // SÉLECTION D'UNE ADRESSE // ==================================================== window.selectAccAddress = async function (addr) { input.value = addr; suggestions.innerHTML = ""; try { const url = `https://data.geopf.fr/geocodage/search?q=${encodeURIComponent(addr)}`; const response = await fetch(url); if (!response.ok) { throw new Error( "Erreur lors du géocodage" ); } const geo = await response.json(); if ( !geo.features || !geo.features.length ) { return; } const coords = geo.features[0].geometry.coordinates; const lon = coords[0]; const lat = coords[1]; // Suppression de l'ancien marqueur if (addressMarker) { map.removeLayer(addressMarker); } // Nouveau marqueur addressMarker = L.marker([lat, lon]).addTo(map); // Calcul de la distance const d = distance( CENTER.lat, CENTER.lon, lat, lon ); // ==================================================== // RECENTRAGE DE LA CARTE // ==================================================== if (d < 150) { map.setView( [lat, lon], 18 ); } else if (d < 500) { map.setView( [lat, lon], 17 ); } else if (d < 1000) { map.setView( [lat, lon], 16 ); } else { map.fitBounds( [ [CENTER.lat, CENTER.lon], [lat, lon] ], { padding: [60, 60] } ); } // ==================================================== // AFFICHAGE DU RÉSULTAT // ==================================================== const result = document.getElementById("result"); if (d < RAYON) { result.innerHTML = ` <span class="ok"> ✔ Adresse dans le cercle </span> <div class="distance"> Distance : ${Math.round(d)} m </div> `; } else { result.innerHTML = ` <span class="ko"> ✖ Adresse hors du cercle </span> <div class="distance"> Distance : ${Math.round(d)} m </div> `; } } catch (error) { console.error( "Erreur géocodage :", error ); } }; })(); 

</script> <p> <a href="https://form.enercoop.org/autoconsommation-collective-a-castelnau-le-lez" class="bouton" > Je rejoins la communauté énergétique </a> </p>

***

## À Villeneuve-lès-Maguelone
Je me renseigne pour savoir si je suis éligible géographiquement :

<a href="#" class="bouton">Vérifier avec mon adresse postale</a> 